SELECT *
FROM film -- This selects all the columms on the films table
SELECT first_name,
    last_name,
    email
FROM customer --  This finds and returns all the columns of firstname, lastname, emails respectively from the customer table